---
import { getAllPublishedPosts } from "../lib/db";
import HomeScreen from "../v2/screens/HomeScreen.astro";

const db = Astro.locals.runtime.env.DB;
const latest = await getAllPublishedPosts(db);
---

<HomeScreen latest={latest} />
